import { Room } from "./room.model";


export interface GuestHouse {
  id: number;
  name?: string | null;
  description?: string | null;
  // rooms?: Room[] | null;
  // createdBy?: string | null;
  // createdDate: string;
}
